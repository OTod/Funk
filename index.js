document.addEventListener('DOMContentLoaded', () => {
    render(main(vDomInitialize()));
});

/**
 * todo: global todo list
 *
 *  -- add modularity
 *  -- split/separate different modules/code by folders
 *
 *
 *
 */
